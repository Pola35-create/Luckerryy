<Window x:Class="Luckerryy.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Luckerryy.ViewModels"
        mc:Ignorable="d"
        Title="Luckerryy" Height="450" Width="800">
    <Grid>
        <ListView ItemsSource="{Binding Lotteries}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Ticket Price" DisplayMemberBinding="{Binding TicketPrice}"/>
                    <GridViewColumn Header="Jackpot" DisplayMemberBinding="{Binding Jackpot}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
